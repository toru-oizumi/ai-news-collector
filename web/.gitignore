dist/
.astro/
node_modules/
# Local Worker secrets for `wrangler dev` (SITE_TOKEN). Never commit.
.dev.vars
.wrangler/
# Generated by `npm run export:web` from the root package — a fresh snapshot of the
# Notion database on every publish, so it is never committed.
src/data/articles.json
